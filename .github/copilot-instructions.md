Project instructions for all coding agents are in `AGENTS.md` at the repository root (glossary,
file map, load order, globals, events, storage keys, rules, parallel-work lanes). Each folder has its
own short `AGENTS.md`; read only the ones for the folders you change. Run `npm test` before committing.
